// import {
//   GET_PROFILE,
//   GET_PROFILES,
//   PROFILE_LOADING,
//   CLEAR_CURRENT_PROFILE
// } from "../actions/types";

const initialState = {};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
